{% extends 'base.html.twig' %}

{% block title %}CCP{% endblock %}

{% block body %}
    <h1>CCP</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ account.id }}</td>
            </tr>
            <tr>
                <th>IBAN</th>
                <td>{{ account.IBAN }}</td>
            </tr>
            <tr>
                <th>Balance</th>
                <td>{{ account.balance }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('account_index') }}">Retour à la liste</a>

    <a href="{{ path('account_edit', {'id': account.id}) }}">Editer</a>

    {{ include('account/_delete_form.html.twig') }}
{% endblock %}
